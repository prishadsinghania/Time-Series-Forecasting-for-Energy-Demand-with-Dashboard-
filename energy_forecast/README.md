# `energy_forecast` package

Installed with **`pip install -e .`** from the repo root.

- `notebook_setup` — `REPO`, `DATA`, `REPORTS` paths derived from the package location (works no matter which folder Jupyter uses as cwd).
- `io_utils` — `save_file()` for CSV exports in wrangling.
